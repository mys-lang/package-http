from http import get

def main():
    headers, data = get("example.com")
    print(headers)
    print(string(data))

    headers, data = get("example.com", 443, secure=True)
    print(headers)
    print(string(data))
